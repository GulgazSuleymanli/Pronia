@model List<Shipping>
@{
    int count = 0;
}


<div class="container pt-3">
    <div class="d-flex justify-content-around">
        <h3>Shippers</h3>
        <a class="btn btn-success" asp-controller="Tag" asp-action="Create">Create</a>
    </div>
    <table class="table table-secondary table-striped text-dark">
        <thead>
            <tr>
                <th scope="col">No</th>
                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">Icon Url</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                count++;
                <tr>
                    <th scope="row">@count</th>
                    <td>@item.Id</td>
                    <td>@item.Title</td>
                    <td>@item.IconUrl</td>
                    <td>
                        <a class="btn btn-primary" asp-controller="Tag" asp-action="Update" asp-route-id="@item.Id">Update</a>
                        <a class="btn btn-danger" asp-controller="Tag" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>